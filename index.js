const express = require("express"); const TelegramBot = require("node-telegram-bot-api");  const app = express(); app.use(express.json());  const bot = new TelegramBot("YOUR_BOT_TOKEN"); const CHAT_ID = "YOUR_CHANNEL_ID";  const TOKEN_CA = "BHSd65jRJz3hnzKtuwnmCWWah3qwNVKZ6bHg4wLmri7i";  app.post("/webhook", (req, res) => {     const data = req.body;      data.forEach(tx => {         bot.sendMessage(CHAT_ID,         `🚀 SatosiAI ALERT\n💰 Amount: ${tx.tokenAmount || 0}`);     });      res.sendStatus(200); });  , (ng"));
